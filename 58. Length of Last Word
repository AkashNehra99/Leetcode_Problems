class Solution {
    public int lengthOfLastWord(String s) {
        int count = 0;
        s= s.trim();
        int b = s.length()-1;
        while(b>=0 && s.charAt(b)!=' ')
        {
            count++;
            b--;
        }
        return count;
    }
}
