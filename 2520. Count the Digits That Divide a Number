class Solution {
    public int countDigits(int num) {
        int x = num;
        int r,d;
        int count = 0;
        while(x!=0){
            r = x%10;
            if(num%r==0)
                count++;
            x=x/10;
        }
        return count;
    }
}
